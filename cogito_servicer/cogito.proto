syntax = "proto3";

package cogito;

service Cogito {
    rpc Complete (Conversation) returns (Status);
}

message Conversation {
    string user_id = 1;
    string conversation_id = 2;
}

message Status {
    string status = 1;
}
